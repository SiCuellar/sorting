//testing for sorting
var assert = require('chai').assert;
var Sort = require('../lib/sorting_suite');

describe('Sort', function() {

  it('should be a function', function() {
    assert.isFunction(Sort);
  });
});
